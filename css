@charset "utf-8";
/* CSS CHUNG CHO TRANG */

body {
    font-family: 'Arial', sans-serif;
}

.navbar {
    padding: 0;
}

.navbar-brand img {
    max-height: 40px;
}

.nav-link {
    font-weight: bold;
    font-size: 0.9rem;
    color: #000;
}

.nav-link:hover {
    color: #555;
}

.btn-link {
    margin-left: 10px;
    font-size: 1.2rem;
}

.form-control {
    border: none;
    border-bottom: 1px solid #000;
    border-radius: 0;
}

.form-control:focus {
    box-shadow: none;
    border-color: #000;
}

.btn-outline-none {
    border: none;
    background: transparent;
    padding: 0;
    font-size: 1.2rem;
}

header .container-fluid {
    max-width: 1440px; /* Kích thước rộng của màn hình */
}

.border-bottom {
    border-bottom: 1px solid #000;
}

.breadcrumb {
    background-color: #f8f9fa;
    margin-bottom: 0;
}

.breadcrumb-item a {
    color: #000;
    text-decoration: none;
}

.breadcrumb-item a:hover {
    text-decoration: underline;
}

.breadcrumb-item.active {
    color: #555;
    font-weight: bold;
}

/* Sidebar Styling */
.accordion-button:not(.collapsed) {
    background-color: #f8f9fa;
    color: #000;
}

.accordion-item {
    border: none;
}

.accordion-button::after {
    filter: invert(0);
}

/* Product Card Styling */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap'); /* Import Google Fonts */

/* Set a consistent height for product cards */
.card {
    height: 470px; /* Đặt chiều cao cố định cho tất cả các thẻ */
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Hiệu ứng khi hover */
    border: none;
    overflow: hidden;
    border-radius: 10px;
}

.card:hover {
    transform: scale(1.05); /* Phóng to thẻ khi hover */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Tạo bóng cho thẻ khi hover */
}

.card img {
    height: 250px; /* Đặt chiều cao cố định cho hình ảnh */
    object-fit: cover; /* Đảm bảo hình ảnh luôn lấp đầy không gian */
    border-radius: 10px 10px 0 0; /* Bo góc phía trên của hình ảnh */
}

.card-title {
    font-family: 'Poppins', sans-serif; /* Sử dụng phông chữ Poppins */
    font-size: 1rem;
    font-weight: bold;
    margin-top: 10px;
}

.card-text {
    font-family: 'Poppins', sans-serif; /* Sử dụng phông chữ Poppins */
    font-size: 0.875rem;
    color: #555;
    margin: 10px 0;
    flex-grow: 1; /* Đảm bảo phần text chiếm hết không gian có sẵn */
}

.btn-primary {
    background-color: #000;
    color: #fff; /* Màu chữ ban đầu */
    border: none; /* Không có viền ban đầu */
    margin-top: 10px;
    font-family: 'Poppins', sans-serif; /* Sử dụng phông chữ Poppins */
    transition: all 0.3s ease; /* Hiệu ứng chuyển đổi mượt mà */
}

.btn-primary:hover {
    background-color: #333; /* Màu nền khi hover */
    border: 1px solid #000; /* Viền đen mỏng khi hover */
    transform: translateY(-2px); /* Nâng nút khi hover */
    --bs-btn-active-bg: ##ffffff;
    --bs-btn-active-border-color: #000000;
    --bs-btn-active-color: #000000;
}

@media (max-width: 767px) {
    .card {
        height: 470px; /* Đặt chiều cao cố định cho tất cả các thẻ */
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        transition: transform 0.3s ease, box-shadow 0.3s ease; /* Hiệu ứng khi hover */
        border: none;
        overflow: hidden;
        border-radius: 10px;
    }

    .card:hover {
        transform: scale(1.05); /* Phóng to thẻ khi hover */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Tạo bóng cho thẻ khi hover */
    }
    
    .card img {
        height: 250px; /* Đặt chiều cao cố định cho hình ảnh */
        object-fit: cover; /* Đảm bảo hình ảnh luôn lấp đầy không gian */
        border-radius: 10px 10px 0 0; /* Bo góc phía trên của hình ảnh */
    }
}

.btn-primary {
    background-color: #000;
    color: #fff;
    border: none;
    border-radius: 25px;
    padding: 10px 20px;
    font-size: 1rem;
    font-weight: bold;
    text-transform: uppercase;
    transition: background-color 0.3s ease;
}

.btn-primary:hover {
    background-color: #333;
    color: #fff;
}

.product-card {
    margin-bottom: 2rem;
}

/* Pagination Styling */
.pagination {
    display: flex;
    justify-content: center;
    align-items: center;
    list-style: none;
    padding: 0;
    margin-left: 327px;
}

.pagination .page-item {
    margin: 0 8px;
}

.pagination .page-link {
    color: #000;
    font-size: 1.1rem;
    font-weight: bold;
    text-decoration: none;
    background: none;
    border: none;
}

.pagination .page-link:hover {
    color: #333;
}

.pagination .page-item.disabled .page-link {
    color: #aaa;
    cursor: default;
}
footer {
    background-color: #000;
    color: #fff;
    padding: 2rem 0; 
}

footer h5 {
    font-size: 1.2rem;
    margin-bottom: 1rem;
    font-weight: bold;
}

footer p {
    margin-bottom: 0.5rem;
    font-size: 0.9rem;
}

footer a {
    color: #fff;
    text-decoration: none;
    transition: color 0.3s;
}

footer a:hover {
    color: #ddd;
}

footer hr {
    margin: 2rem 0;
    border-top: 1px solid #444; 
}

footer .fa-lg {
    font-size: 1.5rem; 
}

footer .text-center p {
    font-size: 0.8rem;
    margin-top: 1rem;
}

.navbar-brand {
    font-family: 'Poppins', sans-serif;
    font-weight: 700;
    font-size: 2rem;
    color: #000; 
    text-transform: lowercase;
    text-decoration: none;
}

.navbar-brand:hover {
    color: #555;
}

/* From uiverse.io by @satyamchaudharydev */
/* removing default style of button */

.form button {
    border: none;
    background: none;
    color: #8b8ba7;
  }
  /* styling of whole input container */
  .form {
    --timing: 0.3s;
    --width-of-input: 200px;
    --height-of-input: 40px;
    --border-height: 2px;
    --input-bg: #fff;
    --border-color: #000000;
    --border-radius: 30px;
    --after-border-radius: 1px;
    position: relative;
    width: var(--width-of-input);
    height: var(--height-of-input);
    display: flex;
    align-items: center;
    padding-inline: 0.8em;
    border-radius: var(--border-radius);
    transition: border-radius 0.5s ease;
    background: var(--input-bg,#fff);
  }
  /* styling of Input */
  .input {
    font-size: 0.9rem;
    background-color: transparent;
    width: 100%;
    height: 100%;
    padding-inline: 0.5em;
    padding-block: 0.7em;
    border: none;
  }
  /* styling of animated border */
  .form:before {
    content: "";
    position: absolute;
    background: var(--border-color);
    transform: scaleX(0);
    transform-origin: center;
    width: 100%;
    height: var(--border-height);
    left: 0;
    bottom: 0;
    border-radius: 1px;
    transition: transform var(--timing) ease;
  }
  /* Hover on Input */
  .form:focus-within {
    border-radius: var(--after-border-radius);
  }
  
  input:focus {
    outline: none;
  }
  /* here is code of animated border */
  .form:focus-within:before {
    transform: scale(1);
  }
  /* styling of close button */
  /* == you can click the close button to remove text == */
  .reset {
    border: none;
    background: none;
    opacity: 0;
    visibility: hidden;
  }
  /* close button shown when typing */
  input:not(:placeholder-shown) ~ .reset {
    opacity: 1;
    visibility: visible;
  }
  /* sizing svg icons */
  .form svg {
    width: 17px;
    margin-top: 3px;
  }

  .custom-breadcrumb .breadcrumb-item + .breadcrumb-item::before {
    content: '/'; /* Dấu phân cách giữa các mục */
    color: #000;  /* Màu sắc của dấu phân cách */
    padding: 0 0.5rem; /* Khoảng cách giữa dấu phân cách và văn bản */
}

.custom-breadcrumb .breadcrumb-item.active::before {
    content: '|'; /* Dấu phân cách cuối cùng là | */
    padding: 0 0.4rem; /* Khoảng cách giữa dấu phân cách và văn bản */
    color: #000; /* Màu sắc của dấu phân cách */
}

/* Ensure the product cards are evenly spaced and responsive */
.product-card .card {
    border: none;
    border-radius: 10px;
    overflow: hidden;
    transition: all 0.3s ease;
}

.product-card .card:hover {
    transform: scale(1.05);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.product-card .card-title {
    font-size: 1rem;
    font-weight: bold;
}

.product-card .card-text {
    font-size: 0.875rem;
    color: #555;
}

.btn-primary {
    background-color: #000;
    border: none;
}

.btn-primary:hover {
    background-color: #333;
}

@media (max-width: 767px) {
    .product-card .card {
        margin-bottom: 20px;
    }
}


